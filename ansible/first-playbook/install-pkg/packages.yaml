- hosts: all
  user: admin02
  become: True
  tasks:
    - name: Install httpd package
      yum:
        name: 
        - httpd
        - nano
        - wget
        - vim
        state: present
        update_cache: True
